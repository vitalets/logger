{
  "name": "@vitalets/logger",
  "version": "1.0.0",
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "lint": "eslint src test --ext .ts",
    "tsc": "tsc --noEmit",
    "test": "mocha -n loader=ts-node/esm test/setup.ts test/*.spec.ts",
    "build": "rm -rf dist && tsc -p tsconfig.build.json"
  },
  "lint-staged": {
    "{src,test}/**/*.ts": "eslint --ext .ts --fix"
  },
  "devDependencies": {
    "@types/mocha": "^9.0.0",
    "@types/node": "^17.0.8",
    "@types/sinon": "^10.0.6",
    "@typescript-eslint/eslint-plugin": "^5.9.0",
    "eslint": "^8.5.0",
    "eslint-plugin-mocha": "^10.0.3",
    "lint-staged": "^12.1.4",
    "mocha": "^9.1.3",
    "sinon": "^12.0.1",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  },
  "license": "MIT"
}
